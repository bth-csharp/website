---
title: "Installationscheck ✅"
description: Nu är vi klara och har installerat alla delar i utvecklingsmiljön, så här långt.
author: grm
revision:
    "2025-10-22": "(A, grm) Första utkastet."
sidebar:
    order: 9997
---

import Task from '@components/Task.astro';
import TaskList from '@components/TaskList.astro';
import RewardMessage from '@components/RewardMessage.astro';

Innan du börjar med kursen, se till att du har installerat följande verktyg.


## Labbmiljön

Om dessa startar så är det ok.

<TaskList>
  <Task completed={false}>Dator med operativsystem</Task>
  <Task completed={false}>Webbläsare, minst Chrome, Firefox Developers Edition</Task>
  <Task completed={false}>Texteditor, VS Code</Task>
  <Task completed={false}>Terminal (WSL2 Ubuntu för windows användare)</Task>
  <Task completed={false}>git är installerat(kolla med git --version)</Task>
  <Task completed={false}>GitHub och konto (kan du logga in är det ok)</Task>
  <Task completed={false}>SSH-nycklar på plats (kunde du pusha i webtec och/eller python är det ok)</Task>
  <Task completed={false}>C# och .NET är installerat minst version 9 (kolla med dotnet --version)</Task>
</TaskList>

## Kursrepo

<TaskList>
  <Task completed={false}>Tillgång till bth-csharp, gör uppgiften i Canvas och en inbjudan ska dyka (kolla genom att du ser `bth-csharp` när du klickar på din användare och organisationer på GitHub)</Task>
  <Task completed={false}>Kursrepo på GitHub med `csharp-abcd25` som template skapat</Task>
  <Task completed={false}>Lokalt git repo skapat (kolla genom att köra `git status` i katalogen `dbwebb-kurser/csharp-<akronym>`)</Task>
</TaskList>

## Kursrepo utvecklingsmiljö

<TaskList>
  <Task completed={false}>Att `.editorconfig` finns (kolla med `ls -la` i ditt lokala gitrepo `csharp-<akronym>`)</Task>
  <Task completed={false}>Att `.gitignore` finns (kolla med `ls -la` i ditt lokala gitrepo `csharp-<akronym>`)</Task>
  <Task completed={false}>Att task är installerat (kolla med `task --version`)</Task>
</TaskList>

<RewardMessage />
